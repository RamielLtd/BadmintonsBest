import styled from "@emotion/styled"

import { CONSTRAIN } from "../utils/typography"

const Constrain = styled("div")`
  margin-left: auto;
  margin-right: auto;
  max-width: ${CONSTRAIN};
`

export default Constrain
